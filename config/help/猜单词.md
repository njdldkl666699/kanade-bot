# 猜单词

修改自插件`nonebot-plugin-wordle`。

### `/wordle [长度] [词典]`

别名：`/猜单词`

参数：

- `长度`：单词长度，默认为5
- `词典`：指定词典，默认为`CET4`

开始一局Wordle猜单词游戏。一个会话中只能进行一局游戏。

### `/词典`

别名：`/wordle_dictionaries` `/猜单词词典` `/词典列表`

列出所有支持的词典。

### `/提示`

别名：`/wordle_hint` `/猜单词提示`

获取这局猜单词的提示。

### `/结束`

别名：`/wordle_stop` `/结束游戏` `/结束猜单词`

结束当前会话的猜单词游戏。