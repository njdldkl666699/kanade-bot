# 命令统计

### `/命令排行 [时间段]`

别名：`/top_commands` `/查看命令排行`

参数：`[时间段]`可选，默认为今天。包括：

- `昨天` `昨日`
- `本周` `这周`
- `本月`
- 数字，表示过去的天数，包含今天。例如`/命令排行 7`表示过去7天的命令排行。
- 其他参数值均解析为今天。

查看指定时间段内的命令使用排行，按使用次数从高到低排序，显示前10条命令。