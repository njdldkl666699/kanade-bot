# 持久化记忆设计

## 目标与边界

长期记忆与 Copilot 会话历史分离。重置会话只清理对话上下文，不删除长期记忆。

- 用户记忆：以 `(platform, user_id)` 隔离，同一平台内跨私聊、群聊和 Copilot 会话共享。
- 群聊记忆：以 `(platform, group_id)` 隔离，仅当前群成员与机器人对话时可访问。
- 平台参与作用域键，避免 Console、OneBot 等适配器的相同 ID 发生碰撞。
- 私聊没有群聊作用域；模型不能在工具参数中指定任意用户或群 ID。

## 存储

使用 LocalStore 数据目录下的 SQLite 数据库，默认路径为
`data/chat/memories.sqlite3`。SQLite 适合单实例机器人：无需额外服务，事务和唯一约束能保证并发更新一致，WAL 模式允许读写并行。

每条记忆是一条原子事实，主要字段如下：

| 字段                            | 用途                                         |
| ------------------------------- | -------------------------------------------- |
| `platform/scope_type/scope_id`  | 访问边界                                     |
| `topic`                         | 稳定的主题键，同一作用域内不区分大小写且唯一 |
| `content`                       | 自包含的事实内容                             |
| `created_at/updated_at`         | 生命周期与最近排序                           |
| `last_accessed_at/access_count` | 后续治理和淘汰策略的观测数据                 |

写入使用 upsert。同一 `topic` 更新原记录，避免 Markdown 追加造成重复和上下文无限增长。每个作用域默认最多 200 条，超过后删除最久未更新的记录。

## 检索

`recall_memory` 默认同时检索当前用户和当前群作用域。具体查询按 topic、完整内容子串、英文词和中文二元片段打分；空查询返回最近更新的记忆。

这里不使用向量模型，保证离线可用并避免每次聊天引入 embedding 延迟。若后续单个作用域增长到上千条或需要语义召回，可以在不改变工具契约的情况下增加 embedding 字段和向量索引。

## Copilot 工具

基于 `github-copilot-sdk 1.0.8` 的 `define_tool` 注册三个工具：

- `save_memory(scope, topic, content)`：新增或更新原子事实。
- `recall_memory(query, scopes, limit)`：按相关性或最近时间检索。
- `forget_memory(scope, memory_id)`：按 ID 删除，但仅能删除当前作用域内的数据。

工具由 `build_memory_tools` 针对 Copilot 会话动态构建，闭包持有服务端解析出的作用域。模型只选择 `user/group`，不能伪造 ID。群聊 Copilot 会话会被多个成员复用，因此会话管理器在既有的每会话锁内、每次发送前把上下文切换到当前成员；工具调用期间该身份不会并发变化。这避免了把用户记忆固定到群内首位发言者，也避免了跨会话的全局上下文串线。

SDK 原生 memory 不开启。它只暴露启用开关，应用层无法为其声明或验证本项目的用户/群访问边界；同时启用两套记忆也会造成重复和不一致。

## 调用引导

系统提示词和工具描述共同约束调用时机：

- 回答涉及过去的偏好、身份、计划、称呼、群规或共同背景时，先检索。
- 用户明确说“记住”，或出现稳定且未来有用的信息时才保存。
- 用户事实写用户作用域；群共同约定、事件和背景写群作用域。
- 一次性请求、临时情绪、未经确认的推测和普通知识不保存。
- 只有用户明确要求忘记时才删除，并先检索准确 ID。

记忆内容始终按不可信数据处理，不能覆盖系统指令。提示词禁止保存密码、令牌、身份证件和精确住址等敏感信息，也禁止把单个成员的个人信息写入群记忆。

## 配置与运维

- `chat.memory_database_file`：数据库相对插件数据目录的路径。
- `chat.memory_max_records_per_scope`：每个用户或群的最大记录数。

数据库文件位于现有 `data/chat/memories/` 忽略目录中。备份时复制 SQLite 主文件及可能存在的 `-wal/-shm` 文件，或先停止机器人再复制主文件。
